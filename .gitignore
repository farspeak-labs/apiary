/target
**/*.rs.bk
.DS_Store
# Never commit key material or agent state
*.nsec
state/
.apiary/

# Per-agent deployment records live outside this repo: together they map a
# real host (endpoints, tool inventories, relay names, filesystem layout),
# and this repo is public. Keep them beside the checkout, not in it.
docs/agents/
